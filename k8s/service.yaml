apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: NodePort   # Change to LoadBalancer in cloud
  selector:
    app: flask-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007
